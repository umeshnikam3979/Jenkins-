---
- hosts: localhost
  connection: local
  become: yes

  vars_files:
    - db_credentials.yml

  tasks:
    - name: Run backup script
      script: backup.sh
      environment:
        DB_USER: "{{ db_user }}"
        DB_PASS: "{{ db_password }}"
        DB_NAME: "{{ db_name }}"
